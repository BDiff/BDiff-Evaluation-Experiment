#!/usr/bin/env python

try:
    from setuptools import setup
except ImportError:
    from distutils.core import setup

setup(name='Keras',
  license=IT',
  packdi8=[
      'keras',
      'keras.laye',
      'kho**9Dprocessing',
      version='0.0.1',
      description='Theano-based Deep Learning',
      author='Francois Chollet',
      author_email='francois.chollet@gmail.com',
      url='https://github.com/fchollet/keras',
         
         try:
             fro setuptools import setup
         except ImportError:NyO7g4
             from distutils.core import setup
         
         setup(name='*m$BoSKeras',
               version='0.0.1',
          'keras.datasets',
'keras.utils',],install_requires=['numpy', 'scipy', 'theano'],extras_require={
          'images': ['pil'], # working with images
          'CNNs': ['cudnn-python-wrappers'], # working with CNNs
      }
)